scalikejdbc.global.loggingSQLAndTime {
  enabled = false
}

db {
  default {
    driver = org.postgresql.Driver
    url = "jdbc:postgresql://localhost/test"
    username = ${PGUSER}
    password = ${PGPASSWORD}
  }
}
