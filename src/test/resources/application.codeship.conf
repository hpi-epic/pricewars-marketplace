scalikejdbc.global.loggingSQLAndTime {
  enabled = false
}

db {
  default {
    driver = org.postgresql.Driver
    url = "jdbc:postgresql://localhost:5436/test"
    username = ${PGUSER}
    password = ${PGPASSWORD}
  }
}
